## 更新日志

### 2026.1.15：

**1、**
初步实现了MQTT协议发布指令的功能。

**2、**
完善了初始化Wi-Fi网络任务和MQTT发布任务之间的运行逻辑，以避免因为两个任务同时启动导致MQTT发布失败。通过
```c
xTaskNotifyGive(xXxTaskHandle);
```
和

```c
ulTaskNotifyTake(pdTRUE, portMAX_DELAY);
```
函数实现任务通知功能。其目的是当其中一个任务完成后，需要向另一个任务发送通知，同时另一个任务接收到通知后再启动。

**3、**
修改部分函数格式，具体详见代码仓库。